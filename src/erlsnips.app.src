%% This is the application resource file (.app file) for the erlsnips
{application, erlsnips,
  [{description, "Some snips for erlang"},
   {vsn, "0.0.1"},
   {registered,[]},
   {applications, [kernel,stdlib,erlsnips]},
   {build_dependencies, []},
   {mod, {erlsnips_app, []}},
   {env, []}
]}.
